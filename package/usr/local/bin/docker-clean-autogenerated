#!/bin/bash

set -e

if [[ "$1" == "--help" || "$1" == "-h" ]]; then
echo "usage: docker-clean-autogenerated
Remove container with auto-generated names (that contains _).
"
exit 0
fi

docker rm $(docker ps -f name=_ -a -q)
