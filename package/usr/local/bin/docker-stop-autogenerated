#!/bin/bash

set -e

if [[ "$1" == "--help" || "$1" == "-h" ]]; then
echo "usage: docker-stop-autogenerated
Stop container with auto-generated names (that contains _).
"
exit 0
fi

docker stop $(docker ps -f name=_ -a -q)
